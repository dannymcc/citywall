<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Plain framework theme; Compose draws the actual UI. No AppCompat/Material XML. -->
    <style name="Theme.CityWall" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/cw_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen (androidx.core:core-splashscreen). Swaps to Theme.CityWall. -->
    <style name="Theme.CityWall.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/cw_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_mono</item>
        <item name="postSplashScreenTheme">@style/Theme.CityWall</item>
    </style>
</resources>
